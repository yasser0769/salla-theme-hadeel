# T006 — Asset classification and theme-owned request sets

> **HISTORICAL BUILD TELEMETRY:** classifications remain useful; any raw-1MB
> publishing conclusion is `SUPERSEDED` by the compressed package contract.

- **Recorded (UTC):** 2026-08-09T14:37Z
- **HEAD:** `eb40134dcda5c147ff03a048ad05b3e11ba8a15b` (HDL-05 changes uncommitted on `codex/hdl-05-performance-bundle-ci`)
- **Method:** source grep of every shipped asset against `src/views/**`, `src/assets/js/**`, `src/assets/styles/**`, plus a search of `node_modules/@salla.sa/twilight-components/dist` and `@salla.sa/twilight/dist` for platform-side references. Sizes from `src/config/bundle-budget.json` (post-T005F tree, 28 files / 1,172,827 raw / 227,413 gzip-9).

## Removed assets (owner-approved, T005F)

| Asset | Raw B | Class |
|---|---:|---|
| `images/fonts/thmanyah/thmanyahsans-{Light,Regular,Medium,Bold,Black}.woff2` (5 files) | 385,492 | **Removed** — permanent owner decision 2026-08-09; see `evidence/font-removal.md` |

## Current 28 assets — classification

### JS/CSS bundles (13 files, 1,131,999 B raw) — all USED (statically proven)

Every bundle is referenced by an `| asset` tag in `src/views/**/*.twig`:

| Asset | Raw B | Referenced from |
|---|---:|---|
| `app.css` | 802,202 | `layouts/master.twig:81` (every page) |
| `app.js` | 129,221 | `layouts/master.twig:258` (every page) |
| `product.js` | 56,127 | product pages (`| asset` ×2) |
| `home.js` | 36,947 | home page |
| `product-card.js` | 31,589 | `layouts/master.twig:61` |
| `add-product-toast.js` | 25,605 | `layouts/master.twig:64` |
| `checkout.js` | 11,727 | cart / thank-you pages |
| `main-menu.js` | 10,522 | `layouts/master.twig:62` |
| `testimonials.js` | 9,957 | testimonials page |
| `digital-files.js` | 5,720 | digital-files page |
| `pages.js` | 5,284 | loyalty / brands pages |
| `wishlist-card.js` | 5,142 | wishlist partial |
| `order.js` | 3,389 | order pages |

### Images (10 files, 38,580 B raw)

| Asset | Raw B | Class | Evidence |
|---|---:|---|---|
| `images/placeholder.png` | 20,664 | **USED** | `master.twig:59` (`theme.settings.set('placeholder', …)`), `components/home/main-links.twig:29` |
| `images/delivery-bro.svg` | 7,573 | **USED** | `pages/thank-you.twig:32` |
| `images/payment_icons/mispay.svg` | 5,796 | **USED** | `pages/product/single.twig:330` |
| `images/payment_icons/tabby.svg` | 2,625 | **USED** | `pages/product/single.twig:317` |
| `images/payment_icons/tamara.svg` | 652 | **USED** | `pages/product/single.twig:304` |
| `images/check.svg` | 824 | **NO STATIC REFERENCE** | Zero hits in `src/` and in `@salla.sa/twilight-components` / `@salla.sa/twilight` dist. May still be referenced by Salla server-rendered markup — rendered-DOM confirmation **NOT VERIFIED** (see below). Registered decision deferred to T009 packet (C5). |
| `images/s-empty.png` | 119 | **RUNTIME-OBSERVED / NO STATIC REFERENCE** | Post-C1/C5 collection capture on draft `1233213199` emitted a platform-generated route-relative `images/s-empty.png` request, which returned 404. The source/public asset is retained; the path-resolution risk is routed to HDL-23/HDL-29. See `evidence/t012-runtime-evidence.md`. |
| `images/s-empty-small.png` | 107 | **NO STATIC REFERENCE** | **NOT VERIFIED** (same). |
| `images/s-empty-square.png` | 95 | **NO STATIC REFERENCE** | **NOT VERIFIED** (same). |
| `images/s-empty-wide.png` | 125 | **NO STATIC REFERENCE** | **NOT VERIFIED** (same). |

### Licence extracts (5 files, 815 B raw)

`*.js.LICENSE.txt` — webpack-emitted third-party licence extracts, gitignored (`/public/*.LICENSE.txt`), regenerated by every production build. **USED (build artifact)**; counted toward the cap because Salla receives them.

## Per-route theme-owned request sets (FR-004, FR-008)

Post-change request sets are now captured on owner-confirmed draft `1233213199`; see
`evidence/t012-runtime-evidence.md` and `evidence/live/draft-1233213199/`. Arabic home with 16 cards and
collection with 5 cards both loaded six theme-local URLs, so no per-card request pattern was observed.
The strict same-collection comparison at two product counts remains **NOT VERIFIED** because the
confirmed catalogue exposed only the five-card collection state and no merchant data was mutated.
Before-change request sets also remain **NOT VERIFIED** because the historical HDL-04 evidence contains
screenshots and narrative but no network log.

## Notes

- `s-empty*` / `check.svg` are small (1,270 B combined); even confirmed-unused removal is negligible against the 172,827 B cap gap and is recorded in the C5 row of `css-font-candidates.md` rather than executed.
