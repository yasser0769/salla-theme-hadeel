# T028 — Automated gates evidence

**Recorded**: 2026-08-10 19:51–19:55 +03, after T021/T022/T027 (phase 2 active) and
the T029 production build + manifest ratchet. All commands run in this working
tree on branch `codex/hdl-06-motion-system`, HEAD `e5f99aed` (uncommitted
working tree).

## Tests-first red witness (HDL-05 suite blocker)

Before the T029 production build, the HDL-05 suite failed — exact red state:

- `node --test tests/performance-bundle-ci.test.mjs` — **fail**:
  "current public/ raw/gzip rows match the committed telemetry manifest" and
  "compressed package passes even though raw public/ exceeds 1,000,000 B"
  failed with `actual: 55, expected: 0`.
- Root cause (verified, not assumed): `node scripts/check-bundle-budget.mjs`
  reported 55 drift errors — the committed manifest was ratcheted at the
  pre-HDL-06 tree, while (a) `public/` on disk was a stale *development* build
  regenerated by a stray webpack watch process (PID 4598, started Aug 7, cwd =
  project root; e.g. recorded `public/app.js` 129221 B vs actual dev 324550 B)
  and (b) every HDL-06 source change (SCSS/JS/Twig migrations, new
  `partials/motion.js` + `01-settings/motion.scss`, deleted
  `partials/anime.js`) was unrecorded.
- Fix (documented HDL-05 workflow, no hand-edited measurements): stopped the
  stray watcher, `npx webpack --mode production`, then
  `node scripts/check-bundle-budget.mjs --update` to ratchet the manifest from
  the verified production build. Reran all affected gates below.

## Final gate results (all green)

| Gate | Result |
|---|---|
| `node --test tests/motion-system.test.mjs` | 107 tests, 107 pass, 0 fail |
| `node --test tests/settings-preset-engine.test.mjs` (HDL-03) | 73 tests, 73 pass, 0 fail |
| `node --test tests/localization-rtl-accessibility.test.mjs` (HDL-04) | 24 tests, 24 pass, 0 fail |
| `node --test tests/performance-bundle-ci.test.mjs` (HDL-05) | 39 tests, 39 pass, 0 fail |
| `node scripts/check-motion-system.mjs --full --json` | 0 errors, phase 2, 11/11 rules ok |
| `node scripts/check-motion-system.mjs --strict --json` | 0 errors, phase 2, 11/11 rules ok |
| `node scripts/check-theme.mjs --json` | 0 errors, 0 warnings (9 findings ok) |
| `node scripts/check-bundle-budget.mjs` | 0 errors; compressed package 479890/1000000 B PASS |
| `git diff --check -- . ':!public'` | clean (no whitespace errors) |

Motion rules enforced at phase 2: `motion-settings`, `motion-resolver`,
`motion-tokens`, `motion-no-timer`, `motion-no-anime`, `motion-reveal`,
`motion-timing`, `motion-utility`, `motion-scroll`, `motion-direction`,
`motion-controller`.

## Not verified here (explicit)

- Live storefront rendering, RTL/focus behavior, and network waterfall remain
  T031–T033 (preview explicitly excluded from this session).
- `git diff --check` excludes `public/` per the task contract; `public/` sync
  is instead proven by `check-theme.mjs --build` (T028 row above / T029).
