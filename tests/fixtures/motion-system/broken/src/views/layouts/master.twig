{# Broken witness: raw merchant value interpolated straight into a class,
   no allowlist, no balanced fallback, no mobile-reduction normalization. #}
<body id="app" class="theme-raed hadeel-motion-{{ theme.settings.get('motion_level', 'balanced') }}">
</body>
